<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Dgaryclientemobile" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Splash Screen Configuration -->
        <item name="android:windowBackground">@drawable/splash_screen_logo</item>
    </style>

    <style name="Theme.Dgaryclientemobile" parent="Base.Theme.Dgaryclientemobile" />

    <!-- Clean theme without background for regular screens -->
    <style name="Theme.Dgaryclientemobile.CleanScreen" parent="Base.Theme.Dgaryclientemobile">
        <item name="android:windowBackground">@color/white</item>
    </style>
</resources>